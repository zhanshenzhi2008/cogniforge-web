# =====================================================================
# cogniforge-web 部署变量
# 本文件不单独部署。将前端和后端 Compose 复制至
# /opt/project/cogniforge/ 后，使用后端仓库提供的 .env.example 创建唯一的 .env。
#
# Web Compose 在 Compose 解析时读取该共享 .env（或 Portainer Stack environment
# variables）。至少必须配置：
#
#   DOMAIN=cogniforge.example.com  # Traefik 路由完整域名
#
# 前端 API 基址是构建时固定的 /api；如需覆盖，构建镜像时传入 Docker build arg：
#   --build-arg API_BASE=https://api.example.com
# =====================================================================
