# =====================================================================
# cogniforge-web 环境变量
# 注意：本服务的环境变量很少，已全部直接写在 docker-compose-web.yml 里，
# 不再单独维护 .env 文件。如需修改默认值，直接编辑 compose 的 environment 块即可。
#
# 当前 compose 里定义的所有变量（按需填值，compose 会自动验证必填项）：
#
#   DOMAIN=cogniforge.example.com      # 必填（${DOMAIN:?}）：Traefik 路由域名
#   NUXT_PUBLIC_API_BASE=/api          # 可选，默认 /api
#   SSR_NAIVE=false                    # 可选，默认 false（Nuxt SSR 开关）
# =====================================================================
