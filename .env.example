# Naive UI SSR Mode
# Set to "true" to enable SSR (server-side rendering) with @bg-dev/nuxt-naiveui.
# Requires: pnpm add @bg-dev/nuxt-naiveui
# When false: lightweight client-only setup (no extra module overhead)
SSR_NAIVE=false
